---
description: Reminder to update README.md when making significant changes to the Gallery project
alwaysApply: true
---

# README 更新规则

当完成以下类型的改动时，必须同步更新项目根目录的 `README.md`：

1. **新增用户可见功能**（新按钮、新面板、新操作方式）
2. **修改操作流程**（按键变更、UI 布局调整）
3. **新增配置项**（元素属性、场景设置）
4. **修改数据结构**（存储位置、文件格式变化）
5. **重大架构变更**（影响用户使用方式的重构）

更新内容包括：
- "使用方法"部分：确保操作说明与实际一致
- "更新日志"部分：在最前面添加当天日期和改动摘要
- "项目结构"部分：如有新文件则更新目录树

不需要更新 README 的情况：
- 纯性能优化（不影响用户操作）
- 内部 bug 修复（不改变功能表现）
- 代码重构（不改变外部行为）
